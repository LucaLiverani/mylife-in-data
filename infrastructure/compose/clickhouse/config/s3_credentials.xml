<clickhouse>
    <!-- Named Collections for S3 Credentials -->
    <!-- These can be referenced in S3Queue, S3 tables, and table functions -->
    <!-- Keeps credentials separate from storage configuration -->

    <named_collections>
        <!-- MinIO inbound bucket - for raw data ingestion -->
        <minio_inbound>
            <url>http://minio:9000/inbound/</url>
            <access_key_id>admin</access_key_id>
            <secret_access_key>minio08062013</secret_access_key>
        </minio_inbound>
    </named_collections>
</clickhouse>
