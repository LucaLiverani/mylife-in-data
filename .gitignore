# Python
*.pyc
__pycache__/
**/__pycache__/
**/.pytest_cache/
**/.mypy_cache/
**/.ruff_cache/
**/venv/
**/.venv/

# Secrets / env — all .env* files are gitignored except *.env.example.
**.env
**.env.local
**.env.development
**.env.production
.env
!**.env.example
client_secrets.json
airflow_variables.json
.spotipy_token_cache
.google_portability_token.pickle
tokens/
**/tokens/
credentials/*.json
credentials/*.txt
!credentials/.gitkeep

# Logs
logs/
**/logs/

# Cloudflare tunnel
**/cloudflared-config.yml
**/.cloudflared/

# dbt
transformations/profiles.yml
transformations/.user.yml
transformations/target/
transformations/dbt_packages/
transformations/logs/

# IDE
.idea/
.vscode/

# Claude Code per-user settings (paths leak local home dir)
.claude/settings.local.json
.claude/settings.json

# Access / deployment values (real VM IP, user, tunnel UUID, etc. live here; gitignored)
ACCESS.md

# Private content workspace
private/

# Legacy data dumps (Google Takeout extracts etc.) — never commit
.legacy-data/

# Scratch / TODO files for next-day sessions — never committed.
TODO.md
TODO_*.md
MAPS_HANDOFF.md
